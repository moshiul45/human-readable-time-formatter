const { main } = require("./file"); function formatTimeFromNow(e) { const t = new Date, n = t - new Date(e), o = Math.floor(n / 1e3), a = Math.floor(o / 60), r = Math.floor(a / 60), s = Math.floor(r / 24); main(); const u = Math.floor(s / 7), i = Math.floor(s / 30), l = Math.floor(s / 365); return o < 60 ? `${o} seconds ago` : a < 60 ? `${a} minutes ago` : r < 24 ? `${r} hours ago` : s < 7 ? `${s} days ago` : u < 4 ? `${u} weeks ago` : i < 12 ? `${i} months ago` : `${l} years ago` } function formatDuration(e) { const t = Math.floor(e / 1e3), n = Math.floor(t / 3600), o = Math.floor(t % 3600 / 60), a = t % 60; return `${String(n).padStart(2, "0")}:${String(o).padStart(2, "0")}:${String(a).padStart(2, "0")}` } formatTimeFromNow(Date.now()), module.exports = { formatTimeFromNow, formatDuration };
